[project]
name = "practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "fastapi[standard]>=0.124.4",
]

[dependency-groups]
dev = [
    "pre-commit>=3.5.0",
    "ruff>=0.15.16",
]
